\begin{frame}{채점 기준: ``채웠는가''가 아니라 ``구체적인가''}
  \begin{itemize}
    \item 8.2의 \textbf{3단계}(감상평 / 일반 지적 / 반박 가능한
      질문)가 그대로 적용
    \item Block A에서 3점 리뷰의 ``근거가 되는 개념''은 Ch02~07
      범위였는데, Block B에서는 \kb{아키텍처-데이터 매칭}이 가장
      높은 확률로 3점짜리 질문을 만들어내는 영역
  \end{itemize}
  \vskip0.3em
  \begin{columns}[T]
    \begin{column}{0.42\textwidth}
      \centering
      \includegraphics[width=0.85\linewidth]{ch16_2_review_decision_tree.png}
      \vskip0.3em
      {\footnotesize ``왜 DL인가'' 판단 나무 — 비정형인가로
      분기 $\to$ 구조-편향 일치 여부 $\to$ 3점 질문}
    \end{column}
    \begin{column}{0.56\textwidth}
      \small
      \begin{itemize}
        \item 읽는 법: ``\textbf{방법론 적절성}''을 채울 때 \textbf{
          왼쪽에서 오른쪽으로 한 번만} 따라가면 됨
        \item \textbf{비정형?} 예 $\to$ \kb{데이터 구조와 편향이
          일치?}
          \begin{itemize}
            \item \textbf{일치}(이미지$\to$CNN, 시퀀스$\to$
              Transformer) $\Rightarrow$ 3점: ``이 구조를
              아키텍처의 어떤 부분이 이용하나?''
            \item \textbf{불일치}(이미지인데 완전연결)
              $\Rightarrow$ 3점: ``Ch10.1 파라미터 공식으로 왜
              이 설계?''
          \end{itemize}
        \item \textbf{오(정형)} $\to$ GBDT·로지스틱회귀와
          \textbf{val에서} 비교했는가? 아니오 $\Rightarrow$ ``
          GBDT를 val에서 돌려봤나요?''
        \item 예/아니오가 \textbf{정해지지 않는} 발표 $\Rightarrow$
          그 \textbf{자체}를 질문으로
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}
