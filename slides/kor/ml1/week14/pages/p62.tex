\begin{frame}{데모의 두 가지 한계 (의도된 것)}
  \begin{itemize}
    \item \textbf{첫째}, \texttt{mean\_vec}가 \textbf{문서의 단어
      순서를 버린다} --- ``양자 컴퓨터''와 ``컴퓨터 양자''는 \textbf{같은
      문서 벡터}. \; 의도된 단순화: 의미 유사도 검색은 ``어떤 단어가
      들어 있는가''를 보고 순서는 잘 보지 않음 (14.2 bag-of-words 한계
      와 같은 이야기).
      \par \textbf{단어 순서까지 살리려면}: 12장 어텐션을 문서 단위로
      돌리는 \kb{문서 임베딩 모델}(실전 RAG가 하는 일).
    \item \textbf{둘째}, 이 데모는 RAG의 \textbf{검색 단계}만 ---
      검색된 문서를 받아 ``이 문서에 기반해 답하라''고 하는
      \textbf{생성 단계}는 LLM의 일이니 Week 17에서.
  \end{itemize}
  \vskip0.4em
  {\footnotesize 실전과의 차이: (a) \texttt{train\_skipgram} 대신
  \textbf{전문 임베딩 모델}, (b) 문서를 \textbf{문장 단위로 잘라}
  (chunking) 벡터화, (c) 수십억 문서에서 빠르게 최상을 찾게
  \textbf{벡터 DB}(FAISS, HNSW 등). \; \kq{원리는 지금 만든 이 20줄과
  같다.}}
\end{frame}
