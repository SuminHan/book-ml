\begin{frame}{실습 노트북 (17.2)}
  \begin{itemize}
    \item 17.1의 \texttt{next\_token\_distribution}
      구현과 \textbf{zero-shot vs few-shot} 출력 대조
      (본문 코드 블록 재현).
    \item \texttt{temperature}를 붙인 장난감 모델 — 같은
      ``사과는'' 프롬프트에서 $T=1.0$ vs $T=3.0$의 20회
      샘플링 결과 비교(같은 프롬프트에서 왜 매번 다른 답이
      나오는가).
    \item \textbf{temperature 곡선} — logits $[2.0, 1.0,
      0.5, 0.2]$에 $T \in [0.2, 3.0]$ 적용 시 최상위 확률
      의 변화(본문 온도 그림).
    \item \textbf{CoT vs 직접 답 확률 시뮬레이션} — 3단계
      추론, 부분 추론 성공률(중간 결과 조건으로 쓸 때 0.9
      vs 없으면 0.5), 4,000회 반복: 직접 $\approx 0.5^3
      = 0.125$, CoT $\approx 0.9^3 = 0.729$.
  \end{itemize}
  {\footnotesize 프롬프팅과 추론을 더 깊이: Stanford
  CS224N(Natural Language Processing with Deep Learning).}
\end{frame}
