\begin{frame}{교차 엔트로피 손실: ``정답이 받은 확률''을 최대화}
  \begin{itemize}
    \item 모델이 토큰 $j$에 확률 $p_j$를 부여했고,
      \textbf{실제로 정답이 $j$}였을 때 그 한 토큰의 손실:
      \[\mathcal{L}_{\text{token}} = -\log p_j\]
    \item 세 가지 값으로 읽기: $p_j = 0.9 \to -\log 0.9
      \approx 0.105$(거의 맞힘, 거의 벌 없음) / $p_j = 0.5 \to
      0.693$(동전 던지기 수준, 중간 벌) / $p_j = 0.001 \to
      6.908$(거의 0 확률에 정답, 큰 벌).
    \item \textbf{왜 로그인가}: (1) 문장 확률 = 토큰 확률의
      \textbf{곱}은 길어질수록 underflow — 로그가 \textbf{곱을
      합}으로. (2) \textbf{감소효용}: 확률 0.1$\to$0.35(3.5배)는
      손실 1.25를 줄이지만 0.5$\to$0.75(1.5배)는 0.41밖에
      줄이지 못함.
    \item 정답 확률이 0.99까지 모이면 그래디언트가 거의 0이
      되어 크게 갱신하지 않고 수렴 — \textbf{softmax + 교차
      엔트로피가 안정적으로 학습되는 이유}(Week 2.3
      로지스틱회귀와 같은 논리).
  \end{itemize}
\end{frame}
