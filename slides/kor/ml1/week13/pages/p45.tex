\begin{frame}{1단계 --- D는 들어오는 링크가 없다}
  \begin{itemize}
    \item D를 가리키는 노드가 없으니 합 $\sum_{j \to D}$ 는 비어 있다:
    \[ PR(D) = \frac{1-d}{N} = 0.125 \]
  \end{itemize}
  \vskip0.5em
  \begin{block}{읽기}
    D는 \textbf{반복을 아무리 돌려도 0.125에 머문다} ---
    ``나를 가리키는 이가 없으니 \textbf{teleport분밖에} 받을 수
    없다''는 뜻.
  \end{block}
\end{frame}
