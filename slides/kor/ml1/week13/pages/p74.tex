\begin{frame}{다음으로 가는 길: ML2 강화학습으로}
  \begin{itemize}
    \item (c)의 ``\textbf{시계열을 그래프 위에 얹는다}''는 생각은
      \kb{ML2의 강화학습}으로 자연스럽게 이어진다.
    \item \kb{MDP}는 ``\textbf{상태를 노드로, 행동을 전환으로} 놓은
      그래프''와 같은 구조를 갖고.
    \item Q-함수 / 가치함수는 그 위에서 ``\textbf{반복해서 값을
      갱신한다}''는 연산을 수행 (ML2 Week 4의 \kb{벨만
      방정식}).
  \end{itemize}
  \vskip0.5em
  \begin{block}{13.4 $\to$ ML2}
    13장의 ``\textbf{그래프 위에서 값을 갱신하는 감각}''이,
    ML2의 ``\textbf{상태-행동 그래프 위에서 가치를 갱신하는
    감각}''으로 이어진다.
  \end{block}
\end{frame}
