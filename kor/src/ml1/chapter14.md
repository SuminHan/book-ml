# Chapter 14. 생성형 모델 맛보기와 총정리 (Generative Models Preview & Review)

2006년, 제프리 힌턴(Geoffrey Hinton)과 루슬란 살라쿠트디노프(Ruslan
Salakhutdinov)는 신경망으로 PCA보다 훨씬 유연한 방식의 차원축소를 할 수 있음을
보였다. 핵심 아이디어는 간단하다: 입력을 압축했다가 다시 원본으로 복원하도록
신경망을 학습시키되, 중간에 **병목**(bottleneck) — 원본보다 훨씬 좁은 은닉층
— 을 강제로 통과시킨다. 신경망이 병목을 통과하고도 원본을 복원해낸다면, 그
병목의 값이 원본 정보를 압축한 유용한 표현이라는 뜻이다. 이 구조가
**오토인코더**(Autoencoder)다.

## 14.1 오토인코더의 구조

두 부분으로 이루어진다:

- **인코더**(Encoder) \\(f\\): 입력 \\(x\\)를 저차원 잠재 표현(latent
  representation) \\(z = f(x)\\)로 압축.
- **디코더**(Decoder) \\(g\\): \\(z\\)로부터 원본을 복원 \\(\hat{x} = g(z)\\).

손실함수는 복원 오차(reconstruction error), 보통 평균제곱오차를 쓴다:

\\[J = \frac{1}{m}\sum_{i=1}^m \|x^{(i)} - g(f(x^{(i)}))\|^2\\]

```python
def autoencoder_forward(x, encoder_weights, decoder_weights):
    z = apply_layer(x, encoder_weights)   # 압축: 입력보다 좁은 은닉층
    x_hat = apply_layer(z, decoder_weights)  # 복원: 다시 원본 차원으로
    return x_hat, z
```

## 14.2 왜 병목이 필요한가

만약 은닉층이 입력과 같거나 더 넓다면, 신경망은 그냥 입력을 그대로 복사하는
항등함수(identity function)를 배워버릴 수 있다 — 그러면 복원 오차는 0이
되지만 아무것도 배운 게 없다. 병목(입력보다 훨씬 좁은 은닉층)을 강제하면,
신경망은 원본을 통째로 저장할 수 없으므로 **정말로 중요한 정보만 압축해서
담아야** 한다.

## 14.3 PCA·EM과의 관계

Chapter 12에서 배운 PCA도 "고차원 데이터를 정보 손실 최소로 저차원으로
압축한다"는 목표는 같았다. 차이는 PCA가 **직선**(선형) 투영만 가능한 반면,
오토인코더는 신경망이므로 **비선형** 압축이 가능하다는 점이다 — 실제로
오토인코더의 인코더가 선형 활성함수만 쓰면, 수학적으로 PCA와 정확히 같은
것을 학습한다는 것이 알려져 있다:

\\[\text{선형 오토인코더} \subset \text{비선형 오토인코더}, \qquad
\text{선형 오토인코더} \approx \text{PCA}\\]

즉 오토인코더는 PCA의 일반화(generalization)라고 볼 수 있다. 비선형
활성함수(ReLU, sigmoid 등)를 인코더/디코더에 추가하면, PCA로는 표현할 수
없는 곡면 모양의 데이터 구조도 압축할 수 있다.

Chapter 13의 EM/GMM과 비교하면 또 다른 각도가 보인다 — GMM의 잠재변수
\\(z\\)는 "어느 클러스터인가"라는 이산값이었지만, 오토인코더의 잠재
표현 \\(z\\)는 연속적인 벡터다. 둘 다 "관측되지 않은 \\(z\\)가 관측된
\\(x\\)를 설명한다"는 같은 틀 안에 있다 — GMM은 EM(반복적 확률 계산)으로,
오토인코더는 역전파(경사하강법)로 그 \\(z\\)를 찾는다는 방법만 다르다.

## 14.4 압축에서 생성으로

오토인코더는 원래 압축(인코딩) 목적으로 만들어졌지만, 흥미로운 질문 하나가
남는다: 병목의 값을 무작위로 하나 골라서 디코더에 넣으면 어떻게 될까? 원본
데이터와 비슷하지만 **존재한 적 없는 새로운 데이터**가 나올 수도 있지
않을까? 이 질문이 바로 "생성형 모델(generative model)"의 시작이다.

오토인코더를 그대로 "생성 모델"로 쓰려고 하면 문제가 있다: 잠재 공간
\\(z\\) 위의 어떤 값들이 "그럴듯한" 데이터로 복원되는지 알 수 없다 — 학습
데이터가 차지하는 영역이 잠재 공간에서 듬성듬성하거나 불규칙할 수 있기
때문이다. **변분 오토인코더**(Variational Autoencoder, VAE)는 이 문제를
잠재 공간이 매끄러운 확률분포(보통 정규분포)를 따르도록 강제해서 해결한다 —
ML2 Chapter 10에서 다룬다. GAN과 Diffusion은 완전히 다른 원리(적대적 학습,
점진적 노이즈 제거)로 같은 목표(그럴듯한 새 데이터 생성)를 이룬다 — ML2
Chapter 11에서 다룬다.

## 14.5 ML1 총정리

이번 학기는 선형/로지스틱회귀의 경사하강법에서 시작해, 생성적 분류(나이브
베이즈/GDA), 거리·마진 기반 모델(kNN, SVM), 트리 앙상블(GBDT), 신경망(역전파,
CNN), 비지도학습(PCA, 임베딩, EM/GMM)까지 왔다.

| 장 | 배운 것 | 핵심 질문 |
|---|---|---|
| Ch02 | 선형·로지스틱회귀 | 연속값과 확률을 어떻게 예측하는가 |
| Ch03 | 나이브베이즈/GDA | 데이터가 어떻게 생성됐는지부터 거꾸로 분류할 수 있는가 |
| Ch04 | kNN/k-means | "가깝다"만으로 예측·군집화할 수 있는가, 그 한계는 무엇인가 |
| Ch05 | SVM | 확률이 아니라 마진으로 경계를 정할 수 있는가 |
| Ch06 | 정규화/모델선택 | 과적합을 손실함수로 어떻게 통제하는가 |
| Ch07 | 트리/랜덤포레스트/GBDT/SHAP | 데이터를 잘 나누는 질문을 어떻게 고르고, 그 예측을 어떻게 설명하는가 |
| Ch08 | 신경망/역전파 | 층을 어떻게 학습시키는가 |
| Ch09 | 학습 기법 | 깊은 신경망이 왜, 어떻게 학습을 실패하는가 |
| Ch10 | CNN 기초 | 이미지의 국소 구조를 어떻게 효율적으로 다루는가 |
| Ch11 | CNN 응용 | 학습된 CNN을 어떻게 재사용하고 더 깊게 쌓는가 |
| Ch12 | PCA/임베딩/PageRank | 정답 없이 데이터·그래프의 구조를 어떻게 찾는가 |
| Ch13 | EM/GMM | 관측되지 않은 변수를 어떻게 추정하는가 |
| Ch14 | 오토인코더 | 압축이 어떻게 생성으로 이어지는가 |

공통된 흐름을 돌아보면: **모델을 정의하고 → 틀린 정도를 손실함수로
수치화하고 → 그 손실을 줄이는 방향으로 파라미터를 조정한다**는 구조가
거의 모든 장에서 반복됐다. ML2는 이 신경망 기초 위에서, 시퀀스(RNN/
Transformer), 대규모 언어모델, 강화학습, 생성형 모델로 응용력을 확장한다.
